[package]
name = "monty_hall_problem_wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

serde = { version = "1.0", features = ["derive"] }

rand = "0.8"
getrandom = { version = "0.2", features = ["js"] }

serde_json = "1.0"
